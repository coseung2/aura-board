# Web (`src/`)

- Checks: `rtk npm run dev`, `rtk npm run typecheck`, `rtk npm run test`,
  `rtk npm run build`.
- For visual changes, clear `.next` and restart dev before browser checks when
  practical.
