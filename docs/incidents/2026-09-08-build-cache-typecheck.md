# 빌드 캐시 변경 검증 중 개발 경로 타입 오류

## 2026-09-16 후속 검증

- 현재 `main` 동기화 후 `npm run typecheck`가 통과해 당시 개발 경로 타입 오류는 재현되지 않았다.
- 빌드 캐시와 cutover manifest 단위 테스트 12개가 통과했으며 Windows에서 생성할 수 없는 symlink 검사 1개만 건너뛰었다.
- 실제 Oracle ARM64 환경의 cold/warm 빌드 시간, Linux `flock`, 캐시 용량 증가는 첫 배포 후 별도로 확인한다.

- 일시: 2026-09-08 KST (Asia/Seoul), Oracle 빌드 최적화 로컬 검증 중.
- 증상: `npx tsc --noEmit`이 `.next/dev/types/validator.ts`의 은행 정기예금 취소 및 거래 정정 경로에서 TS2344로 실패했다.
- 영향: 전체 TypeScript 검사 완료를 주장할 수 없음. 캐시/배포 계약 테스트는 41개 중 24개 통과, Windows 미지원 17개 건너뜀. YAML 파싱·Bash 구문 검사·줄 수 검사 통과.
- 원인: 생성된 경로 검증 타입과 AppRouteHandlerRoutes 사이 불일치 확인. 생성 시점 경쟁 또는 오래된 개발 캐시 여부는 미확인. 은행 소스는 이번 변경 대상이 아니다.
- 대응: 타입 검사 비활성화나 은행 코드 수정 없이 오류를 기록했다. 운영 배포·커밋·푸시를 수행하지 않았다.
- 후속: 개발 서버/타입 생성 상태를 별도로 점검하고 전체 타입 검사 재실행. 첫 Oracle 빌드에서 타입 검사 통과를 반드시 확인한 뒤 배포한다.
