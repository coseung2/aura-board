# 오목 시안 탐색 기록 — 2026-09-15 KST

## 범위와 분류

요청은 오목 시안 생성이다. 오목 페이지(`11 · Game · 오목`, 183:2)에는 이미
구현을 역으로 옮긴 4화면이 있고(2026-09-15에 READY 기준선으로 승격됨), 새 시안은
승인 전 대안이므로 constitution 11절에 따라 EXPLORE로 분류하고 새 섹션
`[EXPLORE] 2026-09-15 · 시안 3안`(197:209)에 담았다. 기존 WIP·컴포넌트·변수는
수정하지 않았다.

- 파일: `I74zYjs7O4bOtFAwahqfza`
- 페이지: `11 · Game · 오목`(183:2)
- 코드 기준: HEAD `51e52ee7`, 작업 트리에 무관한 미커밋 변경 존재
  (`infra/oracle/README.md`, `src/lib/error-log.ts`, `src/lib/error-alert*`)

## 코드 대조

- 웹: `src/components/OmokBoard.tsx`, `src/components/OmokBoard.module.css`
- 모바일: `apps/mobile/components/layouts/OmokBoard.tsx`,
  `apps/mobile/components/layouts/omok/OmokGrid.tsx`
- 판 구조: `grid-template-columns: repeat(15, ...)` + 셀 테두리이므로 착수점은
  격자 교차점이 아니라 셀 중심이다. 돌은 `.stone { inset: 11% }`로 셀의 약 78%다.
- 모바일은 조준 후 확정(`여기에 두기` / `취소`), 웹은 즉시 착수다.

## 생성한 프레임

| 노드 | 이름 | 크기 |
|---|---|---|
| 198:104 | Omok / EXPLORE / A · 교실 대국 / Web / Playing | 1440×900 |
| 201:1571 | Omok / EXPLORE / A · 교실 대국 / Mobile / Playing | 390×844 |
| 205:196 | Omok / EXPLORE / B · 슬레이트 미니멀 / Web / Playing | 1440×900 |
| 206:242 | Omok / EXPLORE / B · 슬레이트 미니멀 / Mobile / Playing | 390×844 |
| 208:288 | Omok / EXPLORE / C · 나이트 펫대국 / Web / Playing | 1440×900 |
| 215:334 | Omok / EXPLORE / C · 나이트 펫대국 / Mobile / Playing | 390×844 |
| 218:381 | Explore Notes | 1440×268 |

세 안 모두 같은 국면(흑 4목, 백이 한쪽 차단, 흑 차례)을 보여 비교가 가능하다.
판과 돌은 기존 `Omok / Board`(185:2)와 `Omok / Stone`(183:59) 인스턴스이며,
B·C의 톤 변경은 인스턴스 오버라이드로만 처리했다. 디태치는 없다.

## 방향별 요지

- **A · 교실 대국**: 현재 웹 민트 셸 유지. 좌측 대국자 카드 + 착수 기록,
  우측 상태·안내·행동. 교실 중계에서 착수 이력이 남는다.
- **B · 슬레이트 미니멀**: 꼬들과 같은 슬레이트 톤. 대국자를 마주보게 배치하고
  가운데 시계. 흰 판으로 태블릿 눈부심을 줄이고 놀이보드 톤을 통일한다.
- **C · 나이트 펫대국**: 다크 판 + 대표펫/관전자 강조. 승부처를 발광으로 알린다.

## 검증

`get_screenshot`과 인라인 `node.screenshot()`으로 6개 프레임과 각 판을 확인했다.
수정한 결함:

1. 인스턴스 `resize()`가 내부 원을 함께 줄이지 않아 돌이 격자보다 컸다. 모든
   돌을 `rescale()`로 셀 크기에 맞췄다.
2. 축소 인스턴스에서 1px 격자선이 디바이스 픽셀 아래로 내려가 절반이 사라졌다.
   보드를 `resize` 대신 `rescale`로 축소하고 선 두께를 보정했다.
3. 조준·호버 표시가 마지막 착수 금색 링과 같은 색이라 구분되지 않았다. A는
   먹색 점선, B는 잉크 점선, C는 시안 발광으로 분리했다.
4. 다크 판에서 흑돌, 흰 판에서 백돌이 배경에 묻혔다. 각각 쿨톤 림과 슬레이트
   테두리를 인스턴스 오버라이드로 넣었다.
5. 착수 기록·최근 착수 좌표가 실제 돌 배치와 달라 셀 좌표로 맞췄다.

검증하지 않은 것: 실제 브라우저·실기기 렌더링, 포커스 순서, 대비 수치 측정,
애니메이션, 프로토타입 동작.

## 미해결

- 상태 커버리지: 매칭 대기, 준비 대기, 종료·재대국, 연결 끊김·복구, 오류 안내가
  EXPLORE에 없다. 방향이 선택된 뒤 그 안으로만 그린다.
- 구현에 없는 제안 요소: 생각 시간 타이머, 착수 기록, 관전 인원 표시. 채택하면
  스냅샷 계약(`contracts/game-platform-v1.schema.json`)과 Rust 엔진 변경이 필요하다.
- 리디자인 방향(A/B/C) 승인은 아직 없다. 세 안 모두 EXPLORE에 남아 있다.
- 구현을 옮긴 4화면은 2026-09-15에 `[READY] 현행 구현 기준선`으로 승격됐다.
  이는 "지금 동작하는 화면"의 기준선이며 리디자인 방향을 정한 것이 아니다.
  방향이 채택되면 그 안을 REVIEW→READY로 올리고 이 기준선을 DEPRECATED로 옮긴다.
- 두 세트가 서로 다른 방향이라는 사실은 그대로다. 선택 후에야 canonical 후보가
  하나로 모인다.

## 롤백

섹션 197:209만 제거하면 이 작업 전 상태로 돌아간다. 기존 WIP 프레임, 컴포넌트,
변수는 변경하지 않았다.
