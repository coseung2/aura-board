# 놀이보드 Figma ↔ 구현 반영 점검 — 2026-09-15 KST

## 점검 범위와 방법

깃 동기화 후(`origin/main` 67커밋 fast-forward, HEAD `7ab49da6`) Figma 파일
`I74zYjs7O4bOtFAwahqfza`를 읽기 전용으로 조회하고, `LAYOUT_RELEASES`에 등록된
놀이(PLAY) 레이아웃과 대조했다. 화면 확인은 `get_metadata`와 `get_screenshot`
으로 수행했고, 상호작용·접근성·실기기 동작은 확인하지 않았다.

`get_metadata`를 nodeId 없이 호출하면 페이지 목록이 `90 · 탐색 시안 보관`
한 건만 반환된다. 실제 페이지는 노드 ID로 직접 접근해야 열린다. 아래 표의
페이지는 모두 개별 노드 조회로 존재를 확인한 것이다.

## 놀이보드별 Figma 커버리지

| 놀이보드 | 코드 stage | Figma 페이지 | 상태 |
|---|---|---|---|
| 노래 맞히기 | development | `10 · 교사 화면`(39:90), `20 · 학생 화면`(39:91), v2 플로우(100:2, 101:2 등) | 교사 7 / 학생 9 화면 + v2 교사 플로우 존재 |
| 꼬들 | development | `14 · Game · 꼬들`(146:2), `15 · 컴포넌트`(146:3) | 11화면 WIP, READY/DEPRECATED 섹션 비어 있음 |
| 오목 | development | `11 · Game · 오목`(183:2), `12 · 컴포넌트`(183:3) | 4화면 WIP · 2026-09-15 생성 |
| 스피드게임 | development | `13 · Game · 스피드게임`(196:104), `13.1 · 컴포넌트`(196:105) | 2화면 WIP · 2026-09-15 생성 |
| 그림자연합 | development | `16 · Game · 그림자연합`(198:1606), `16.1 · 컴포넌트`(198:1607) | 2화면 WIP · 2026-09-15 생성 |

점검 시작 시점에는 `OFFICIAL_GAME_CATALOG` 5종 중 노래 맞히기와 꼬들만 Figma
자산이 있었다. 오목·스피드게임·그림자연합 캔버스를 이 작업에서 생성해 5종
모두 디자인 자산을 갖추게 했다. 페이지 ID는 `figma-page-index.json`에 있다.

## 확인된 반영 상태

### 꼬들 — 코드가 시안을 따라간 항목

`docs/design/figma-migration-plan-2026-09-14.md`가 기록한 배경 전환과 대기실 펫은
코드에도 들어가 있다.

- 뉴트럴 슬레이트 배경: `kordle-gameplay.css` 122·135행이
  `linear-gradient(155deg, #e8edf4 0%, #cfd8e6 100%)`로 시안과 동일하다.
- 카드 분리선과 그림자: 29~32행 `border: 1px solid #cbd5e1` + `box-shadow`.
- 밝은 배경용 본문색 `#0f172a`가 헤더·상태 텍스트에 적용됐다.
- 대기실 대표펫: 웹은 `KordleWaitingRoom.tsx`가 HTTP 스냅샷의
  `representativePet`을 presence 행에 병합하고, 모바일은
  `KordleBoard.tsx` 67~70행이 동일 데이터로 슬라임 스프라이트를 그린다.
- 라이브 토스트 하단 고정: `kordle-result-and-responsive.css` 30~32행
  `position: fixed; bottom: 18px`.

### 꼬들 — 시안에 없고 코드에만 있는 것

`KordleLiveFeed.tsx`(모바일)와 `kordle-live-feed.ts`는 4069e0fc 이후 추가된
라이브 피드다. Figma `14 · Game · 꼬들` WIP에는 T1 교사 실시간 화면의 토스트
레일만 있고, 모바일 라이브 피드 화면은 없다.

### 노래 맞히기 — v2 시안과 현재 교사 설정의 차이

v2 교사 자동 출제 화면(`100:2`)과 `SongGuessPoolPicker.tsx`를 비교하면 구조는
같지만 표현이 다르다.

| 항목 | v2 시안 | 현재 구현 |
|---|---|---|
| 듣기 구간 | 버튼 2개(하이라이트 / 도입) + 설명문 | `<select>` 드롭다운 |
| 문제 수 | 버튼 4개(5·10·15·20문제) | `<select>` 드롭다운 |
| 단계 표기 | `1 · 노래 분류` / `2 · 듣기 구간` / `3 · 문제 수` | 번호 없는 레이블 |
| 곡 풀 시각화 | 숫자 + 진행 바(`pool` / `pool-active`) | 텍스트 `곡 풀 N곡` |
| 예상 진행 시간 | `약 6–8분` 카드 | 없음 |
| 우측 요약 | `이번 게임` 패널에 조건·정답 기준·답변 방식 집약 | 좌우 분리 없이 단일 흐름 |

v2 교사 로비(`101:2`)의 `18 / 24명` 입장 카운터와 `최소 1명 이상이면 시작할 수
있어요` 조건 문구는 eebcb14e에서 T3 라이브 입장 카운트로 반영됐다.

## 정리해야 할 상태 불일치

1. ~~꼬들 `[READY]`가 비어 있고 canonical 화면이 없다.~~ 해결. 사용자 승인 후
   11화면을 READY로 승격했다. 다른 네 보드도 함께 승격했다.
2. ~~README와 Figma 안내가 "앱에는 아직 적용되지 않았습니다"라고 적고 있다.~~
   해결. README는 2026-09-08 산출물임을 밝히고 구현 코드를 기준으로 지목하도록
   고쳤고, Figma `53:12`와 `53:4`도 반영 확인 날짜와 v2 기준을 담도록 갱신했다.
3. ~~`figma-manifest.json`이 v1 페이지만 담고 있다.~~ 해결. 꼬들 페이지와
   아카이브 페이지를 `pages`에 추가하고, v2 화면은 페이지가 아니므로
   `flowV2Frames`로 분리해 기록했다.
4. ~~오목·스피드게임·그림자연합은 Figma 자산이 없다.~~ 해결. 세 게임 모두
   구현 기준으로 캔버스를 생성했다. 다만 모든 화면이 `[WIP]`이고 상태 커버리지는
   각 매니페스트의 `gaps`에 남겼다.
5. 오목에 EXPLORE 시안 3안을 추가했다(`[EXPLORE] 2026-09-15 · 시안 3안`,
   197:209). 기록은 [`omok-explore-2026-09-15.md`](omok-explore-2026-09-15.md).
   WIP 4화면과 EXPLORE 3안이 병존하므로 방향 선택 전까지 오목 canonical 후보는
   하나로 모이지 않는다. **미해결.**

## v2 교사 설정 반영 (2026-09-15)

`SongGuessPoolPicker`의 듣기 구간과 문제 수를 `<select>`에서 눌린 상태를 갖는
버튼으로 바꾸고, 단계 번호(`1 · 2 · 3`), 곡 풀 진행 바, 예상 진행 시간을 더했다.
예상 시간은 문자열을 박아 넣지 않고 `estimateSongGuessDuration`이 권위 규칙의
30초 응답 창(`SONG_GUESS_ANSWER_WINDOW_MS`)에서 계산한다. 같은 상수를 스냅샷
검증기의 리터럴 `30000` 자리에도 넣어 규칙이 갈라지지 않게 했다.

## 확인하지 않은 것

정적 스크린샷과 CSS/TSX 대조만 수행했다. 실제 브라우저 렌더링, 실기기 동작,
포커스 순서, 색 대비 수치, 애니메이션은 확인하지 않았다. 꼬들 승리 토스트의
폭죽·상승 애니메이션은 CSS 키프레임에만 있고 Figma는 정지 상태만 담고 있다.

## 오목 캔버스 생성 (2026-09-15)

오목은 시안이 전혀 없어서 마이그레이션할 대상이 없었다. 그래서 꼬들과 같은
방식으로 현재 구현에서 역으로 캔버스를 만들었다. 색과 치수는 추정하지 않고
`src/components/OmokBoard.module.css`에서 그대로 가져왔다. 판 #d9a65e, 테두리
12px #ad7a42, 격자 rgba(74,46,22,.46), 마지막 착수 #ffca48, 셸 그라디언트
`#effbf7 → #e7f4ff → #f7efff`가 그 예다.

변수는 primitives 23개와 semantic 11개로 나누고, semantic은 alias만 갖는다.
컴포넌트는 `Omok / Stone`(5변형), `Omok / PlayerRow`(4변형), `Omok / Board`
세 개이고, 화면은 모두 인스턴스로 조립했다. 대표펫 자리는 저장소의
`public/creatures/slimes/{blue,green}/idle.gif` 첫 프레임을 4배 nearest 확대해
실제 스프라이트로 채웠다.

돌 배치에서 한 가지를 바로잡았다. 처음에는 격자 교차점에 돌을 놓았는데,
구현은 `grid-template-columns: repeat(15, 1fr)`의 각 칸에 `place-items: center`로
돌을 그린다. 그래서 판의 격자선과 화점, 돌 좌표를 모두 칸 중앙 기준으로 고쳤다.

화면 4개(웹 대국/매칭/결과, 모바일 대국)는 모두 `[WIP]`이다. 무승부, 기권 확정,
관전자, 연결 끊김, 태블릿 브레이크포인트는 아직 없고 `docs/design/omok/figma-manifest.json`의
`gaps`에 남겼다. READY 승격은 사람 승인이 필요하므로 하지 않았다.

## 스피드게임·그림자연합 캔버스 생성 (2026-09-15)

두 게임의 색 체계가 서로 반대라서 변수 구성도 다르게 잡았다.

스피드게임은 전용 팔레트가 없다. `src/styles/speed-game.css`가 `--color-accent`
처럼 앱 전역 토큰만 참조한다. 그래서 게임 전용 primitives를 새로 만들지 않고
`App · Foundations` 컬렉션 20개를 만들어 `src/styles/base.css` 값을 담았다.
같은 토큰을 쓰는 다른 레이아웃이 나중에 재사용할 수 있다. 화면은 교사 라운드
진행과 학생 답변 제출 2개이고, 교사는 제시어를, 학생은 순서 번호만 보는 구현의
역할 분기를 그대로 옮겼다.

그림자연합은 반대로 앱 토큰을 전혀 쓰지 않는 어두운 전용 팔레트를 갖는다.
`.shadow-alliance-game`의 커스텀 속성에서 primitives 18개를 뽑고 semantic 11개를
alias로 연결했다. 판 #08090d, 패널 #141720, 금색 지령 #e4c778, 흑 연합 #7180a4,
백 연합 #f0e5c3이 그 값이다.

폰트에서 한 가지를 확인했다. CSS는 `Nanum Myeongjo`를 먼저 요청하지만 이 파일에
없어서, 추정하지 않고 `listAvailableFontsAsync`로 확인한 뒤 CSS 폴백인
`Noto Serif KR`을 썼다. 브랜드 마크의 `Cinzel`은 파일에 있어 그대로 사용했다.
이 사실은 Design Notes와 매니페스트에 기록했다.

페이지 번호가 겹치는 문제도 정리했다. 꼬들이 이미 14/15를 쓰고 있어서 새로 만든
그림자연합을 16/16.1로 옮겼다. 꼬들 번호는 기존 문서와 매니페스트가 참조하므로
건드리지 않았다.

## 페이지 인덱스를 저장소에 남긴 이유

`get_metadata`를 nodeId 없이 호출하면 페이지가 한 개만 반환된다. 이 때문에 v2
플로우 페이지(99:3, 112:2)를 찾는 데 노드 ID를 여러 번 추측해야 했다. 같은 낭비가
반복되지 않도록 전체 페이지와 변수 컬렉션 ID를
[figma-page-index.json](figma-page-index.json)에 기록했다.

## READY 승격 (2026-09-15)

사용자 승인을 받아 다섯 보드 모두 READY 섹션을 채웠다. 승격 근거는 구현 대조이며
각 화면을 해당 CSS와 맞춰 확인했다.

| 보드 | READY 화면 | 근거 |
|---|---|---|
| 노래 맞히기 | 20 (교사 7 · 학생 6 · 공통·진입 4 · 개요 3) | v2 페이지가 canonical |
| 꼬들 | 11 | `kordle-gameplay.css` 대조 |
| 오목 | 4 | `OmokBoard.module.css` 대조 |
| 스피드게임 | 2 | `speed-game.css` + `base.css` 대조 |
| 그림자연합 | 2 | `shadow-alliance-game.css` 대조 |

### 승격 과정에서 고친 것

꼬들에서 S4(영문)와 S5(패배 결과)가 좌표 `1980,1774`에 완전히 겹쳐 있었다.
결과 모달 오버레이 3개도 `kordle-result`라는 같은 이름을 써서 어느 화면에 속하는지
알 수 없었다. 화면을 한 줄로 재배치하고 오버레이 이름에 소유 화면을 붙였다.

배경 탐색은 C(뉴트럴 슬레이트)가 채택되어 이미 코드에 반영된 상태였다. C를
`ADOPTED`로 표기해 REVIEW에 근거로 남기고, A/B와 이전 5색 그라디언트는
DEPRECATED로 옮겼다. 노드는 삭제하지 않았다.

노래 맞히기는 v1과 v2가 동시에 canonical처럼 보이는 상태였다. v2가 보드 진입과
공통 상태까지 담아 더 완전하므로 v2를 canonical로 확정하고, v1 교사·학생 페이지를
`99 · Archive ... (superseded by 91)`로 표기했다. 2026-09-08 승인 이력은 그대로 남는다.

`99 · Archive · 랜덤뽑기 탐색` 페이지에 노래 맞히기 교사 화면 2개가 함께 있는 것을
발견해 이름을 `랜덤뽑기 + 노래 맞히기 교사 탐색`으로 고쳤다. 처음에 노래 맞히기
전용으로 잘못 바꿨다가, 실제로 두 내용이 함께 있음을 확인하고 되돌렸다.

비어 있던 스피드게임·그림자연합 컴포넌트 페이지도 채웠다. 화면에서 반복되던 패턴을
추출해 `Speed / Button`(5변형), `Speed / ScoreRow`(2변형),
`Shadow / StatusPill`(3변형), `Shadow / Button`(2변형), `Shadow / TeamHeader`(2변형)를
만들었다.

### 오목 READY의 성격

오목 페이지에 제가 만들지 않은 `[EXPLORE] 2026-09-15 · 시안 3안`이 있었다. A 교실
대국, B 슬레이트 미니멀, C 나이트 펫대국 세 방향이고 Explore Notes에 "방향이 정해지면
선택된 안으로만 나머지 상태를 그린다"고 적혀 있다. 즉 리디자인 방향은 아직 미승인이다.

제 READY 화면은 이 세 안 중 무엇도 아니라 현재 구현을 그대로 옮긴 것이다. 그래서
섹션 이름을 `[READY] 현행 구현 기준선`으로 두고 Design Notes에 "이 READY는 지금
동작하는 화면의 기준선이며 리디자인 방향은 미승인"이라고 명시했다. 방향이 채택되면
그 안을 READY로 올리고 이 기준선을 DEPRECATED로 옮기면 된다.

### 여전히 확인하지 않은 것

정적 스크린샷과 코드 대조만 했다. 상호작용, 포커스 순서, 색 대비 수치, 애니메이션은
검증하지 않았다. 각 매니페스트의 `gaps`에 남은 미작성 상태(오목의 무승부·관전자,
스피드게임의 모바일 프레임, 그림자연합의 참가 대기·최종 결과 등)는 그대로 남아 있다.
READY는 그려진 화면이 구현과 일치한다는 뜻이며 상태 커버리지가 완전하다는 뜻은 아니다.
