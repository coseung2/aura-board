# Historical task records

This directory contains implementation history that is still referenced by a
small number of source comments and operational documents. It is not the
current workflow and new work should not create phase-by-phase task artifacts.

Use `AGENTS.md` for working rules and `docs/verification-checklist.md` for
verification. Historical records may be removed once their remaining source or
documentation references have been replaced by current documentation.
